#include<stdio.h>
int main()

//arthmetic operator
/*
{
    int a,b,c;
    printf("enter the values of a and b");
    scanf("%d%d",&a,&b);
    c=a+b;
    printf("the addition of a and b is=%d\n",c);
    c=a-b;
    printf("the subtraction of a and b is=%d\n",c);
    c=a*b;
    printf("the multiplication of a and b is=%d\n",c);
    c=a/b;
    printf("the division of a and b is=%d\n",c);
    c=a%b;
    printf("the madelo of a and b is=%d\n",c);
    return 0;
}
*/


//retation operator
/*
{
    int a,b;
    a=4;
    b=4;

    printf("a == b is %d\n ",a==b);
    printf("a != b is %d\n ",a!=b);
    printf("a < b is %d\n ",a<b);
    printf("a > b is %d\n ",a>b);
    printf("a >= b is %d\n ",a>=b);
    printf("a <= b is %d\n ",a<=b);
}
*/


//logical operator
/*
{
    int a = 5, b = 5,c=10;


    printf("(a == b) && (c > b)  is %d \n",(a == b) && (c > b) );
    printf("(a == b) && (c < b)  is %d \n",(a == b) && (a < b) );
    printf("(a == b) && (c < b)  is %d \n",(a == b) && (c > b) );
    printf("(a != b) && (c > b)  is %d \n",(a != b) && (c > b) );
    printf("(a != b) && (c < b)  is %d \n",(a != b) && (c < b) );
    printf("!(a == b)  is %d \n",!(a == b));
    printf("!(a != b)  is %d \n",!(a != b));


  return 0;
}
*/

//bitwise operator

{
    int a,b;
    a=49;
    b=35;

    printf("a&b is %d\n",a&b);
    printf("a|b is %d\n",a|b);
    printf("a^b is %d\n",a^b);
    printf("a<<b is %d\n",a<<b);
    printf("a>>b is %d\n",a>>b);



}
